name: Skill Security Scan

# Scans AI agent skills in .agents/skills/ with NVIDIA SkillSpector. ADVISORY / NON-GATING:
# these are first-party skills that legitimately run shell/gh/git/template operations, so
# SkillSpector (correctly, for an untrusted third party) rates them HIGH and pins its risk
# score at 100 (exit 1) by design. Those inherent findings cannot be removed without gutting
# the skill, so rather than maintain a per-finding allowlist this workflow reports every
# finding to the job summary and to code scanning for human review and NEVER fails the PR.
# Novel-pattern safety comes from that human review plus the optional LLM semantic scan.
#
# Two scans run: a deterministic STATIC scan (--no-llm) that produces the summary + SARIF,
# and, when an API key is configured, an LLM semantic scan rendered as a separate advisory
# section. Neither blocks the PR. See .agents/skills/AGENTS.md.
#
# SkillSpector publishes no git tags and is not on PyPI, so we pin a commit SHA and
# emit a non-blocking warning when upstream main has moved past the pin.

on:
  pull_request:
    # Run on ready PRs only. Draft PRs still emit opened/synchronize, but the
    # job-level `if` below skips them; marking ready fires ready_for_review.
    types: [opened, synchronize, reopened, ready_for_review]
    paths:
      - '.agents/skills/**'
      - '.github/workflows/skill-scan.yml'
      - '.github/scripts/skillspector-report.py'
  push:
    branches: [main]
    paths:
      - '.agents/skills/**'
      - '.github/workflows/skill-scan.yml'
      - '.github/scripts/skillspector-report.py'
  workflow_dispatch:
    inputs:
      branch:
        description: 'Branch or ref to scan. Leave empty to scan the branch this run was dispatched from.'
        required: false
        type: string

permissions:
  contents: read
  security-events: write   # upload SARIF to GitHub code scanning

env:
  # Pinned SkillSpector commit. Bump after reviewing the warning from the
  # "Check for newer SkillSpector version" step below.
  SKILLSPECTOR_REF: a5092dd9b9521ff57a9b53612bb129ce78019002

jobs:
  skill-scan:
    name: SkillSpector
    runs-on: ubuntu-latest
    # Skip draft PRs; push and workflow_dispatch have no PR context so they always run.
    if: ${{ github.event_name != 'pull_request' || github.event.pull_request.draft == false }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          # Manual runs may target any branch via the `branch` input; empty falls
          # back to the dispatching/triggering ref (normal PR/push behavior).
          ref: ${{ inputs.branch }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install SkillSpector (pinned)
        run: |
          set -euo pipefail
          git clone --filter=blob:none https://github.com/NVIDIA/SkillSpector.git /tmp/skillspector
          git -C /tmp/skillspector checkout "$SKILLSPECTOR_REF"
          pip install -e /tmp/skillspector
          skillspector --version

      - name: Check for newer SkillSpector version (warning only)
        continue-on-error: true
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          latest=$(gh api repos/NVIDIA/SkillSpector/commits/main --jq .sha)
          if [ "$latest" != "$SKILLSPECTOR_REF" ]; then
            echo "::warning title=SkillSpector update available::Pinned $SKILLSPECTOR_REF but upstream main is $latest. Review changes and bump SKILLSPECTOR_REF in .github/workflows/skill-scan.yml."
            {
              echo "### ⚠️ SkillSpector update available"
              echo ""
              echo "- Pinned: \`$SKILLSPECTOR_REF\`"
              echo "- Upstream \`main\`: \`$latest\`"
              echo ""
              echo "Review upstream changes, then bump \`SKILLSPECTOR_REF\` in \`.github/workflows/skill-scan.yml\`."
            } >> "$GITHUB_STEP_SUMMARY"
          else
            echo "✅ SkillSpector pin is current (\`$SKILLSPECTOR_REF\`)." >> "$GITHUB_STEP_SUMMARY"
          fi

      # STATIC scan: deterministic, static-only (--no-llm), --format json (the richest
      # output). The render step derives both the job summary and the SARIF from this run.
      # Advisory: a HIGH raw score (exit 1) is expected for first-party skills and must not
      # fail the job, so the SkillSpector exit code is ignored (|| true).
      - name: Scan skills (static)
        id: scan
        run: |
          set -uo pipefail
          echo "Running deterministic static scan (--no-llm; advisory)."
          skillspector scan .agents/skills/ --no-llm --format json --output skillspector.json || true

      # LLM scan: full semantic analysis, rendered as a separate advisory section. Its
      # findings are nondeterministic across model/prompt drift, so they are surfaced for
      # human review only. Self-skips when no API key is configured. continue-on-error
      # keeps a provider hiccup from failing CI (the whole workflow is non-gating anyway).
      - name: Scan skills (advisory, LLM)
        id: scan_llm
        continue-on-error: true
        env:
          # Provider config supplied via org/repo settings. The left-hand names are the
          # fixed env vars SkillSpector reads; the ${{ }} sources are namespaced org
          # variables/secrets. When the API key is absent this step self-skips and the
          # gate runs static-only — so the gate works with zero secrets.
          SKILLSPECTOR_PROVIDER: ${{ vars.SKILLSPECTOR_PROVIDER || 'openai' }}
          SKILLSPECTOR_MODEL: ${{ vars.SKILLSPECTOR_MODEL }}              # bare model id; empty = provider default
          OPENAI_BASE_URL: ${{ vars.SKILLSPECTOR_OPENAI_BASE_URL }}      # OpenAI-compatible base, must end at /v1
          OPENAI_API_KEY: ${{ secrets.SKILLSPECTOR_OPENAI_API_KEY }}
        run: |
          set -uo pipefail
          if [ -z "${OPENAI_API_KEY:-}" ]; then
            echo "SKILLSPECTOR_OPENAI_API_KEY not set — skipping LLM advisory scan (gate is static-only)."
            exit 0
          fi
          echo "Running LLM semantic analysis (advisory; provider: $SKILLSPECTOR_PROVIDER, model: ${SKILLSPECTOR_MODEL:-provider default}, base: ${OPENAI_BASE_URL:-api.openai.com/v1})."
          # Advisory only: never fail the build on the LLM scan's exit code.
          skillspector scan .agents/skills/ --format json --output skillspector-llm.json || true

      - name: Render report (job summary + SARIF)
        if: ${{ !cancelled() }}
        run: |
          set -euo pipefail
          advisory=()
          [ -s skillspector-llm.json ] && advisory=(--advisory skillspector-llm.json)
          python3 .github/scripts/skillspector-report.py \
            skillspector.json skillspector.sarif .agents/skills \
            "${advisory[@]}" \
            >> "$GITHUB_STEP_SUMMARY"

      - name: Upload SARIF to code scanning
        if: ${{ !cancelled() && hashFiles('skillspector.sarif') != '' }}
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: skillspector.sarif
          category: skillspector
